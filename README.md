# Taller: API REST de Inventario con Python y Flask 

Sistema para administrar el inventario de una pequeña tienda mediante una API REST desarrollada en Python con Flask y un programa consumidor interactivo.

##  Autores
* **Thomas:** Desarrollo de la API (`api.py`).
* **Edgar:** Desarrollo del cliente y menú interactivo con `requests` (`cliente.py`).

---

##  Requisitos Previos
Asegúrate de tener instalado Python y las bibliotecas necesarias. En la terminal ejecuta:
```bash
pip install flask requests